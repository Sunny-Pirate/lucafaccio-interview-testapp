import '../styles/globals.scss'
import type {AppProps} from 'next/app'


const LucaFaccioTestApp = ({Component, pageProps}: AppProps) => {

    return (
        <Component {...pageProps} />
    );
};

export default LucaFaccioTestApp;
